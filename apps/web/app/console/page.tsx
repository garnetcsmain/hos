import { HosDashboard } from "@/app/components/HosDashboard";

// The live operations console. The marketing/explainer landing page now lives
// at "/"; the working Family Reunification Map moved here so first-time
// visitors meet the explanation before the live tool.
export default function ConsolePage() {
  return <HosDashboard />;
}
