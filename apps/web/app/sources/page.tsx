import { ModulePlaceholder } from "@/app/components/ModulePlaceholder";

export default function SourcesPage() {
  return (
    <ModulePlaceholder
      title="Incoming Sources"
      description="Incoming data streams, source volume, and trust metadata coverage."
    />
  );
}
