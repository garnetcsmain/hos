I have everything I need. The critical mother-persona finding is confirmed: the notifications queue is coordinator-gated (`assertCoordinator`), the family notification's `recipient` is a redacted contact that's never actually used to reach anyone, and the only family-facing signal is public search showing a status change from "Abierto" to "Resuelto" — with no detail. The mother has no way to read the "Coincidencia verificada" message addressed to her. She would have to keep manually re-searching her own case number to notice the status flipped to "Resuelto," which itself doesn't tell her if her son is alive, injured, hospitalized, or deceased.

This is the core honesty/utility tension in D4 from the user's seat. I now have enough to write a sharp, grounded review.

# Venezuelan Mother / Shelter Volunteer / Coordinator Review: HOS-2026-002

## My Recommendation (per decision)
- D1: **GREEN LIGHT** - The "why matched" evidence chain is real, in Spanish, and accent/nickname-aware (Pepe to Jose), and the contradiction flags stop a shared common name from sending me to the wrong morgue. This is the part I'd actually trust.
- D2: **GREEN LIGHT** - I never see the database; what I feel is that it loads fast on my cheap Android with patchy data, and that's exactly what SQLite-on-the-edge buys me. No friction for any of the three of us.
- D3: **RESHAPE** - Open public intake is right and frictionless, but the single shared coordinator token means any volunteer with the password can confirm "found alive/deceased" on anyone, with no name attached to who decided. As a mother that terrifies me; as a coordinator I want my decision to be *mine*.
- D4: **RESHAPE** - The honesty is genuinely good (it never lies that it texted me), but in-app-only means the message "Coincidencia verificada para tu hijo" is written *to me* and I can literally never read it. It sits in a coordinator inbox I can't open. For me, the mother, D4 currently delivers nothing.

## Key Findings

**F1 (D4) - The family notification is addressed to the family but the family cannot read it. Severity: CRITICAL.**
`buildFamilyNotification` (verification.ts:23) writes a warm Spanish message — *"Una organización autorizada verificó una posible coincidencia para [nombre]... Un coordinador se pondrá en contacto"* — with `recipient` set to my redacted phone. But the only endpoint that lists notifications (`/api/notifications`) calls `assertCoordinator` (notifications/route.ts:12), and the only UI that renders them is `MessagesInbox`, labeled *"vista de coordinación."* I, the mother, have no account and no inbox. The message about my own son exists in a room I'm locked out of. The system is honest that it didn't SMS me — but it also built a notification I'll never see and a promise ("un coordinador se pondrá en contacto") that nothing in the code enforces or tracks. *If I'm wrong:* if the real workflow is that a human coordinator phones me using the un-redacted contact and the in-app note is purely their internal worklist, then this is fine — but then stop addressing the message to "su familiar" as if it were for me, and show me, somewhere, that someone is obligated to call.

**F2 (D4) - My only family-facing signal is a status word with no meaning attached. Severity: HIGH.**
The one thing I *can* do without an account is re-run the public search and watch my case go from "Abierto" to "Resuelto" (SearchPanel, IntakeForms.tsx:235). That word does not tell me whether my son is *con vida*, *herida*, *hospitalizada*, or *fallecida* — and `toPublicMissing` deliberately strips condition. So I see "Resuelto" and I don't know if I should feel relief or collapse. In a crisis that ambiguity is its own cruelty, and I have to manually re-search over and over on patchy data to even catch it. *If I'm wrong:* acceptable only if a human always reaches me before the status flips publicly; but resolution and notification happen in the same atomic transaction (verification.ts:82-116), so the public "Resuelto" can appear before any human has dialed my number.

**F3 (D3) - One shared token means no name is attached to the most consequential decision in the system. Severity: HIGH.**
`assertCoordinator` checks one env secret (auth.ts). The verify form *asks* for an org and optional name (MatchConsoleParts.tsx:316-327), but those are free-text and unauthenticated — anyone holding the shared token types "Cruz Roja" and confirms my son is deceased. As a coordinator I want my real verification on the record and theirs off mine. As a mother, "una organización autorizada verificó" means nothing if "authorized" is one password shared across every shelter laptop. *If I'm wrong:* tolerable for a single trusted 5-person cell in week one; indefensible the moment two agencies share the deployment.

**F4 (D3) - Dev-open default is a loaded gun for a low-resource deployment. Severity: HIGH.**
If `HOS_COORDINATOR_TOKEN` is unset, verify/notify/timeline are fully OPEN (auth.ts:16-22) with only a console warning nobody reads. The exact teams who'll run this — volunteers on a borrowed VPS, no DevOps — are the most likely to deploy unconfigured. Then anyone on the internet can confirm matches and read redacted family contacts. *If I'm wrong:* fine if deploy is always gated behind a checklist that refuses to boot without the token — but the code ships the dangerous default, not a fail-closed one.

**F5 (D1) - The evidence chain genuinely lands, and the contradiction flags are the trust-maker. Severity: LOW (this is praise with one ask).**
Each factor renders as a labeled Spanish bar with plain detail — *"3 year difference (12 vs 15)," "Sex differs," "Found 4d after last seen"* (engine.ts details + EvidenceChain, MatchConsoleParts.tsx:158). The hard-negative flags (sex mismatch x0.35, age gap >30y x0.4) show up as red `ShieldAlert` notes, so a coordinator sees *why* a strong name match got cut. This is the difference between "the computer says 82%" and "I can see for myself." The one gap: the score is a bare *"82%"* with no calibration — 82% of *what*? A coordinator under pressure may read it as "82% sure it's him." *If I'm wrong:* if coordinators are trained that the number is a sorting aid not a probability, fine; nothing in the UI says so.

**F6 (D1) - "Resuelto" status and the public list let me confirm my family member without anyone calling. Severity: MEDIUM.**
Public search shows found-person `givenName` + `ageBand` + `city` + `status` for anyone (search.ts). A desperate mother who knows the city and a first name can eyeball the found list herself and "decide" a match in her own head — bypassing the whole human-verify gate that D1 exists to protect. The system can't stop false hope I generate myself from the open list. *If I'm wrong:* the PII minimization (no contact, no precise location, coarse age band) is genuinely good and probably the right trade vs. hiding the list entirely — but the false-hope vector is real and unaddressed.

## My concrete scenario + would-I-use-it

**Me: María, mother, Maracaibo, $40 Android, 2 bars of signal.**
I tap *"No puedo localizar a mi familia,"* no account needed — good, I'm in. I file my son "José Luis," 15, last seen near the bridge. The form is all in Spanish, the fields make sense, and it tells me my case number and *"0 coincidencias todavía, seguiremos comparando"* — honest, I appreciate that it doesn't pretend. **This part I'd use and trust.**

Three days later a shelter volunteer enters a found boy, "Pepe," 14, same city. The engine folds Pepe to José, sees age 1 apart, same city, same sex — high score, no contradiction flag. A coordinator opens the evidence chain, sees every reason in plain Spanish, and confirms. The system writes *"Coincidencia verificada para José Luis"* — **to me.**

**And I never see it.** There's no SMS (the system is honest it can't). There's no inbox for me. The only thing I can do is re-type my case number into the public search, on my 2 bars, again and again, until one day it says "Resuelto." It doesn't say alive. It doesn't say which shelter. It doesn't say who to call. I'm a grieving mother staring at the word "Resuelto" with no idea if I just won or lost. **That I would not call working.** The honesty is real and I respect it — but honesty about a message I can't read is small comfort.

**Me: Yusmary, shelter volunteer, 60 found people to enter tonight.** Intake is fast, Spanish, sex defaults to "U" so I'm not forced to guess — good. My worry: I'll over-trust the green 80%+ scores and rubber-stamp, because I'm exhausted and the bar is *right there*. The flags help, but nothing makes me slow down on a high score. **I'd use it, and I'm exactly the over-trust risk the proposal names.**

**Me: coordinator.** The console is the best part of this whole thing — side-by-side reports, weighted evidence, contradiction alerts, a verify panel that forces me to write down my evidence. **I'd use this daily.** My objection is F3: my careful confirmation and some half-asleep volunteer's confirmation are signed with the same shared password, and I can't tell mine from theirs in the audit.

## Confidence Score
**0.78.** High because I read the actual code paths a real user touches — the evidence chain renders as described, the notification truly is coordinator-gated, the auth gate truly is dev-open, the public status truly is bare. Not higher because: (1) I'm inferring the *intended* offline phone-call workflow around D4 rather than seeing it specified — if a strong human-callback SOP exists, F1/F2 soften; (2) I haven't watched a real low-connectivity mother use the search-and-refresh loop, so the "Resuelto with no meaning" pain is reasoned, not observed; (3) I didn't load-test SQLite under two shelters writing at once, so D2's edge-fit is taken on faith from the code, not measured.

## What I Don't Know
- **Is there an out-of-band human-callback step?** The whole verdict on D4 hinges on whether a coordinator is *required* and *tracked* to phone me. The code queues a note; it doesn't model an obligation. I can't see one.
- **Will the public "Resuelto" status appear to me before a human reaches me?** Same transaction resolves the report and queues the notification — so quite possibly yes, and that ordering is the difference between hope and harm.
- **What does a family do when the match is "Fallecida"?** Nothing in the strings or flows handles the death case gently for the family; condition is stripped from public view and there's no compassionate path. This is the highest-stakes user moment and I can't find it handled.
- **Who actually holds the coordinator token, and how many people share it** in the real Venezuela deployment — that single fact decides whether D3 is "fine for now" or "dangerous now."
- **Is the score calibrated against anything?** Cold-start, hand-tuned weights, no labeled data — I don't know if "80%" means 80% of real reunifications or just "this pair scored high relative to others." A coordinator's trust depends on which.

Relevant files: `apps/web/app/lib/services/verification.ts` (family notification built but coordinator-gated), `apps/web/app/api/notifications/route.ts` (`assertCoordinator` gate), `apps/web/app/lib/http/auth.ts` (dev-open default), `apps/web/app/lib/domain/projections.ts` (condition stripped from public view), `apps/web/app/components/MatchConsoleParts.tsx` (evidence chain UI), `apps/web/app/components/MessagesInbox.tsx` (coordinator-only inbox), `apps/web/app/components/IntakeForms.tsx` (family search = status-word-only signal).
